#include "msp.h"

void LED_init()
{
    P1->DIR |= BIT0;//设置为输出
}

void LED_toggle()
{
    P1->DIR ^= BIT0;//设置为输出
}

void soft_ware_delay(int n)
{
    while(n--);
}

void main(void)
{
	WDT_A->CTL = WDT_A_CTL_PW | WDT_A_CTL_HOLD;		// stop watchdog timer

	while(1)
	{

	}
}
